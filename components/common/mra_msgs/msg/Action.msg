# Action, as produced by Teamplay (or test software), consumed by ActionPlanning

uint8 type # enum ActionType
string actionparams # json string
# actionparams is to be mapped to action-specific settings, as optional overrules w.r.t. configuration
