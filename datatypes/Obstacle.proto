syntax = "proto3";

package MRA.Datatypes;

import "datatypes/Pose.proto";

message Obstacle
{
    // typically on the floor, so z,rx,ry unused
    Pose position = 1;
    Pose velocity = 2;
    double radius = 3;
}

