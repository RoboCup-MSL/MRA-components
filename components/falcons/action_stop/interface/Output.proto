syntax = "proto3";

package MRA.FalconsActionStop;

import "datatypes/ActionResult.proto";

message Output
{
    MRA.Datatypes.ActionResult actionresult = 1; // always PASSED
    bool stopMoving = 2; // always TRUE
    bool ballHandlersEnabled = 3; // relay input
}
