# An obstacle is typically on the floor, so z, rx, ry are unused.

geometry_msgs/Pose position  # Current position of the obstacle
geometry_msgs/Twist velocity # Current velocity of the obstacle
float32 radius               # Radius of the obstacle
